apiVersion: v1
kind: Secret
metadata:
  name: platform-secrets
  namespace: platform
type: Opaque
# In production, use sealed-secrets or external-secrets
# For local dev, values are patched via Kustomize overlay
stringData:
  postgres-password: "placeholder"
  secret-key-base: "placeholder"
