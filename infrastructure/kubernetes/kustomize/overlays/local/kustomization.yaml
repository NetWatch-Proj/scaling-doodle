apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

secretGenerator:
  - name: platform-secrets
    namespace: platform
    behavior: replace
    literals:
      - postgres-password=localdev123
      - secret-key-base=local-secret-key-change-in-production
